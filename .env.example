# ============================================================
# 环境变量配置文件示例（生产环境）
# 使用方法：复制为 .env.production 并填写实际值
# ============================================================

# ==================== 基础配置 ====================
# 应用版本
VERSION=1.0.0

# 域名配置
DOMAIN=example.com
HTTP_PORT=80
HTTPS_PORT=443

# SSL 证书路径（本地目录）
SSL_CERT_PATH=./ssl

# 数据存储路径
DATA_PATH=./data

# ==================== 数据库配置 ====================
DB_NAME=qmzg_prod
DB_USER=qmzg_admin
DB_PASSWORD=your_strong_password_here_123456

# ==================== JWT 密钥配置 ====================
# 使用以下命令生成随机密钥：
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

JWT_SECRET=your_jwt_secret_key_here_min_32_characters
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key_here_min_32_characters
JWT_REFRESH_EXPIRES_IN=30d

# ==================== CORS 配置 ====================
CORS_ORIGIN=https://example.com

# ==================== 前端配置 ====================
VITE_API_URL=/api

# ==================== Dify AI 配置 ====================
DIFY_API_URL=https://api.dify.ai/v1
DIFY_API_KEY=
DIFY_CHAT_APP_KEY=
DIFY_STORY_APP_KEY=
DIFY_EMOTION_APP_KEY=
DIFY_TUTORING_APP_KEY=
DIFY_TUTORING_EVALUATE_APP_KEY=
DIFY_TUTORING_SUMMARY_APP_KEY=
DIFY_TIMEOUT=30000

# ==================== DeepSeek AI 配置 ====================
DEEPSEEK_API_KEY=
DEEPSEEK_API_URL=https://api.deepseek.com

# ==================== 智谱 AI 配置 ====================
ZHIPU_API_KEY=

# ==================== 腾讯云 OCR 配置 ====================
# 注意：这个功能需要腾讯云服务，如果没有可以留空
# 不影响其他功能的使用
TENCENT_SECRET_ID=
TENCENT_SECRET_KEY=
TENCENT_OCR_REGION=ap-beijing

# ==================== 邮件服务配置 ====================
# 推荐使用：QQ邮箱、163邮箱、Gmail
SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_USER=your-email@qq.com
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=启蒙之光 <your-email@qq.com>

# ============================================================
# 重要提示：
#
# 1. 必须配置的项目（否则无法启动）：
#    - DB_PASSWORD
#    - JWT_SECRET
#    - JWT_REFRESH_SECRET
#
# 2. 建议配置的项目：
#    - DOMAIN（生产环境域名）
#    - CORS_ORIGIN（前端域名）
#    - 至少一个 AI 服务配置（Dify/DeepSeek/智谱）
#
# 3. 可选配置项目：
#    - TENCENT_* （OCR 功能，没有账号可以不配置）
#    - SMTP_* （邮件功能，可以后期配置）
#
# 4. 生成强密码的方法：
#    方法1（Node.js）：
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
#    方法2（在线工具）：
#    https://www.random.org/strings/
#
# 5. 本文件使用后：
#    cp .env.example .env.production
#    编辑 .env.production 填写实际值
#    chmod 600 .env.production  # 限制文件权限
# ============================================================
