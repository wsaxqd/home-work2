# 测试环境配置文件
# 警告：此配置仅用于自动化测试，不要在生产环境使用！

# 服务器配置
NODE_ENV=test
PORT=3001

# 数据库配置 - 测试数据库
DB_HOST=localhost
DB_PORT=5432
DB_NAME=qmzg_test
DB_USER=postgres
DB_PASSWORD=postgres

# JWT 配置 - 测试专用密钥
JWT_SECRET=test-secret-key-for-integration-testing-only-do-not-use-in-production
JWT_EXPIRES_IN=7d

# AI 服务配置 - 测试环境
OPENAI_API_KEY=test-key-placeholder
ZHIPU_API_KEY=test-key-placeholder

# Redis 配置 - 测试环境
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# 日志配置
LOG_LEVEL=error

# 测试特定配置
TEST_MODE=true
SKIP_AUTH_CHECK=false
